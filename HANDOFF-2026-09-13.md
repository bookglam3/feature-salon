# Handoff — 2026-09-13

Stopping point for the `create_booking_with_services` input-trust fix.
Everything below is decided; nothing needs re-litigating.

---

## BLOCKED ON

`supabase-FUNCTION-BASELINE-2026-09-11.sql` is **still the placeholder**
(1167 bytes, header + paste marker only, no `CREATE OR REPLACE FUNCTION`).

The function definition exists **only in the live database**. It is in no
tracked file, so it is also the only rollback artifact — which is why it
has to land before the function is touched.

Seven paste attempts failed (chat and editor both). Next attempt should be
`psql` straight to file, no clipboard in the path:

```bash
psql "$DATABASE_URL" -Atc "SELECT pg_get_functiondef(p.oid) FROM pg_proc p \
JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' \
AND p.proname='create_booking_with_services'" \
  >> supabase-FUNCTION-BASELINE-2026-09-11.sql
```

Verify:

```bash
grep -c 'CREATE OR REPLACE' supabase-FUNCTION-BASELINE-2026-09-11.sql   # expect 1
```

`check_slot_available` and the `services` DDL are missing from the repo too,
but fetch them **separately afterwards** — only `create_booking_with_services`
is needed to write the diff, and a smaller payload is less likely to mangle.

---

## READY TO BUILD ONCE UNBLOCKED — the input-trust fix

**Migration (one commit, with the client diff):**

- `price` / `name` / `duration_minutes` / `price_is_from` read server-side
  from `services`. Guard 8 is set-based (`jsonb_to_recordset` + `NOT EXISTS`),
  so restructure it to **return columns**, not only validate.
- `p_line_items` shrinks to `{ service_id, sort_order }`.
- `v_total_duration` sums the **server-side** durations.
- `price_is_from` → `COALESCE(s.price_is_from, false)`.
- **Do not port the client's `?? 30` default.** NULL duration or price →
  `INVALID_SERVICE_DURATION` / `INVALID_SERVICE_PRICE`.
  Counts confirmed on live: `null_dur = 1` (service `tst`, £4, Messenger
  Studio — test data, becomes unbookable, acceptable), `null_price = 0`.
- New guards:
  - `p_date_time` in the future, 2 min grace → `DATE_IN_PAST`
  - not more than 2 years ahead → `DATE_TOO_FAR`
  - not on a `closed_dates` entry → `SALON_CLOSED`.
    `closed_dates.date` is **`DATE NOT NULL`**, `UNIQUE(salon_id, date)`.
    Compare in the salon's zone: `(p_date_time AT TIME ZONE v_timezone)::date`.
    Add `v_timezone` via `SELECT ... INTO` folded into Guard 1,
    `COALESCE(timezone, 'Europe/London')` — Guard 1 currently only does
    `IF NOT EXISTS (SELECT 1 FROM salons ...)`, and some older rows have a
    NULL timezone.
  - `jsonb_array_length(p_line_items) <= 20` → `TOO_MANY_LINE_ITEMS`
- `NO_STAFF_CONFIGURED` replaces `SLOT_UNAVAILABLE` when the salon has zero
  active staff, so a newly onboarded salon gets a truthful error.

**Do NOT touch:** `FOR UPDATE` locking, the inline/oracle drift check, the
oracle call itself, or either INSERT's ordering.

**Client diff ships in the SAME commit** — `app/book/[slug]/page.tsx:444`,
`lineItems` → `{ service_id: s.id, sort_order: i }`. Half of it alone breaks
live bookings: `selTime`/line items feed
`fromZonedTime(\`${date}T${time}:00\`)`, and the signature changes shape.

**The closed-date guard is NEW behaviour.** The booking page references
`closed_dates` zero times today and `slot-availability.ts` doesn't either —
nothing filters closed dates client-side. So a customer can currently pick a
closed day and would hit `SALON_CLOSED` from a slot the picker offered them.
Ship it with the picker filtering closed dates, or at minimum the mapped
error below.

**Error mappings** (client, `app/book/[slug]/page.tsx` — the existing handler
maps `SLOT_UNAVAILABLE` and `SLOT_CHECK_DRIFT_DETECTED` and drops everything
else into a raw `"Booking failed: " + msg`):

| Code | Message |
|---|---|
| `NO_STAFF_CONFIGURED` | This salon isn't accepting online bookings yet. |
| `SALON_CLOSED` | The salon is closed on that date. Please pick another day. |
| `DATE_IN_PAST` | That time has already passed. Please pick a later slot. |
| `DATE_TOO_FAR` | That date is too far ahead. Please pick a nearer day. |
| `TOO_MANY_LINE_ITEMS` | Too many services selected. |
| `INVALID_SERVICE_DURATION` / `INVALID_SERVICE_PRICE` | That service isn't available to book right now. |

`search_path` **is** pinned on `create_booking_with_services` (`public`) —
that concern is closed for this function.

---

## WHY IT MATTERS

`p_line_items` carries `price` straight from the client into
`appointment_services`. `create-payment-intent` then reads the price back
**from that table** ([route.ts:45](app/api/create-payment-intent/route.ts#L45))
rather than from the request body — a correctly-built defence that is reading
a number the attacker chose one step earlier.

```
client price → appointment_services.price → create-payment-intent → Stripe charge
```

So a crafted payload sets what Stripe actually charges, and the 2% platform
fee with it. This is a live money path with two paying salons on it.

---

## ALSO QUEUED

- **`search_path` unpinned on 7 admin `SECURITY DEFINER` functions:**
  `get_admin_role`, `has_admin_role`, `is_admin_role`, `is_any_admin`,
  `is_super_admin`, `delete_old_audit_logs`, `expire_old_invites`.
  A `SECURITY DEFINER` function without a pinned `search_path` is a
  privilege-escalation vector.
- **Loyalty `loyalty_start_at` fail-closed fix — before enabling for any real
  salon.** The `loyalty_progress` view currently counts *all* history
  (`COALESCE(c.last_redeemed_at, '-infinity')`), and `loyalty_cards` is empty,
  so every client resolves to `-infinity`. Combined with the `completed_at`
  backfill, switching the programme on would instantly owe rewards to a large
  share of Anita's ~640 clients. Revised migration (column backfilled to
  `now()`, `NOT NULL`, `'infinity'` fallback for salons with no settings row)
  was drafted and reviewed — apply that first, then test 1/5 on salon
  `0056ccd1` only.
- **Dashboard times render in browser-local, not the salon's timezone**
  (12 sites). Real bug, deliberately deferred so a regression stays
  unambiguous after the 12-hour format change.
- **4 salons carry `subscription_plan='business'`** with live Stripe subs on a
  *different* Stripe account than Anita's — check before touching.
- **Anita's period ends 25 Sep.** If Stripe says Starter, the webhook will
  overwrite `subscription_plan` and re-lock her calendar. Today's DB fix was
  manual; the webhook is still the authority.

---

## DONE TODAY (do not re-investigate)

| | |
|---|---|
| RLS | Eight permissive policies dropped (gift_cards, discount_codes, clients, sales_agents anon; staff ×4 authenticated `USING(true)`; payments "Allow all"; sms_opt_outs "Allow all"; 3 duplicate login_logs) |
| Deploy | **Vercel Instant Rollback from Aug 24 undone** — it was silently discarding every deploy, which is why 14 commits "looked deployed" while the live site stayed old. First thing to check next time live ≠ repo. |
| `93725b5` | 12-hour time format across dashboard + booking UI |
| `cf2e405` | 12-hour format across email, WhatsApp, SMS, push |
| `28df339` | Fabricated 147+ counter and its `/api/stats/count` endpoint removed |
| `e3c7b42` | Hardcoded five stars removed from John's testimonial |
| `706f422` | Admin plan dropdown writes `subscription_plan` (also fixed `PLAN_OPTIONS`, which contained a non-existent `"premium"` tier that would have locked salons out of everything) |
| `db9556e` | Dashboard badge reads `subscription_plan` via `PLAN_LABELS` — badge and gate now resolve from one map |
| DB (manual) | Anita Love Hair `subscription_plan` corrected `starter` → `pro` |
| `1833481` | This baseline placeholder |
